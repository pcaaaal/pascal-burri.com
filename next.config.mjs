/** @type {import('next').NextConfig} */
const nextConfig = {
	reactStrictMode: true,

	// Turn on for static export
	// output: 'export',
	// images: {
	// 	unoptimized: true,
	// },
};

export default nextConfig;
